const ACTION_TYPES = {

};

const ACTIONS = {

};

function RegistryTypeReducer (state = 0, action) {
  switch (action.type) {
    default:
      return state
  }

}

module.exports = {
  ACTIONS,
  RegistryTypeReducer,
};