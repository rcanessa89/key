import { colors } from '../../constants';

const DocumentSearchStyles = {

    pageContainer : { 
        width : '50%', 
    },

    searchBar : {
        position: 'relative',
        color: 'whitesmoke',
        borderColor: 'whitesmoke',
        textAlign: 'center',
        borderRadius: 5,
        padding: 15,
        borderLeftWidth: 1,
        borderRightWidth: 1,
        borderTopWidth: 1,
        borderBottomWidth: 1,
    },

};

export default DocumentSearchStyles;